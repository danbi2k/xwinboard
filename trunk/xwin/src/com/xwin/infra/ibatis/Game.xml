<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE sqlMap
	PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">
	
<sqlMap namespace="Game">

	<typeAlias alias="Game" type="com.xwin.domain.game.Game" />
	
	<resultMap id="GameResultMap" class="Game">
		<result property="id" column="ID"/>
		<result property="homeTeam" column="HOME_TEAM"/>
		<result property="awayTeam" column="AWAY_TEAM"/>
		<result property="winRate" column="WIN_RATE"/>
		<result property="drawRate" column="DRAW_RATE"/>
		<result property="loseRate" column="LOSE_RATE"/>
		<result property="status" column="STATUS"/>
		<result property="gameDate" column="GAME_DATE"/>
		<result property="type" column="TYPE"/>
		<result property="homeScore" column="HOME_SCORE"/>
		<result property="awayScore" column="AWAY_SCORE"/>
		<result property="result" column="RESULT"/>
		<result property="leagueId" column="LEAGUE_ID"/>
		<result property="handy" column="HANDY"/>
		<result property="leagueName" column="LEAGUE_NAME"/>
		<result property="leagueImage" column="LEAGUE_IMAGE"/>
		<result property="betStatus" column="BET_STATUS"/>
		<result property="winMoney" column="WIN_MONEY"/>
		<result property="drawMoney" column="DRAW_MONEY"/>
		<result property="loseMoney" column="LOSE_MONEY"/>
	</resultMap>
	
	<select id="selectGameById" resultMap="GameResultMap" parameterClass="string">
		SELECT g.*, l.NAME as LEAGUE_NAME, l.IMAGE as LEAGUE_IMAGE,
		
			(SELECT SUM(MONEY) FROM tbl_betting_game bg, tbl_betting b WHERE bg.BETTING_ID = b.ID AND bg.GAME_ID = g.ID AND GUESS = 'W') AS WIN_MONEY,
			(SELECT SUM(MONEY) FROM tbl_betting_game bg, tbl_betting b WHERE bg.BETTING_ID = b.ID AND bg.GAME_ID = g.ID AND GUESS = 'D') AS DRAW_MONEY,
			(SELECT SUM(MONEY) FROM tbl_betting_game bg, tbl_betting b WHERE bg.BETTING_ID = b.ID AND bg.GAME_ID = g.ID AND GUESS = 'L') AS LOSE_MONEY
		FROM tbl_game g, tbl_league l where g.LEAGUE_ID = l.ID AND g.ID = #value#
	</select>
	
	<select id="selectGameList" resultMap="GameResultMap" parameterClass="map">
		SELECT
			g.*, l.NAME as LEAGUE_NAME,
			l.IMAGE as LEAGUE_IMAGE,
			(SELECT SUM(MONEY) FROM tbl_betting_game bg, tbl_betting b WHERE bg.BETTING_ID = b.ID AND bg.GAME_ID = g.ID AND GUESS = 'W') AS WIN_MONEY,
			(SELECT SUM(MONEY) FROM tbl_betting_game bg, tbl_betting b WHERE bg.BETTING_ID = b.ID AND bg.GAME_ID = g.ID AND GUESS = 'D') AS DRAW_MONEY,
			(SELECT SUM(MONEY) FROM tbl_betting_game bg, tbl_betting b WHERE bg.BETTING_ID = b.ID AND bg.GAME_ID = g.ID AND GUESS = 'L') AS LOSE_MONEY
		FROM tbl_game g, tbl_league l
		WHERE g.LEAGUE_ID = l.ID
		<isNotNull prepend="AND" property="homeTeam">
			g.HOME_TEAM LIKE #homeTeam#
		</isNotNull>
		<isNotNull prepend="AND" property="awayTeam">
			g.AWAY_TEAM LIKE #awayTeam#
		</isNotNull>
		<isNotNull prepend="AND" property="type">
			g.TYPE = #type#
		</isNotNull>
		<isNotNull prepend="AND" property="leagueId">
			l.ID = #leagueId#
		</isNotNull>
		<isNotNull prepend="AND" property="status">
			g.STATUS = #status#
		</isNotNull>
		<isNotNull prepend="AND" property="statusList">
			<iterate prepend="" property="statusList" open=" (" close=") " conjunction="OR">
				g.STATUS = #statusList[]#
			</iterate>
		</isNotNull>
		<isNotNull prepend="AND" property="betStatus">
			g.BET_STATUS = #betStatus#
		</isNotNull>
		<isNotNull prepend="AND" property="fromDate">
			g.GAME_DATE > #fromDate#
		</isNotNull>
		<isNotNull prepend="AND" property="fromDate">
			<![CDATA[
			g.GAME_DATE >= #fromDate#
			]]>
		</isNotNull>
		<isNotNull prepend="AND" property="toDate">
			<![CDATA[
			g.GAME_DATE <= #toDate#
			]]>
		</isNotNull>
		ORDER BY GAME_DATE DESC
		<isNotNull prepend="LIMIT" property="fromRow">
			#fromRow#, #rowSize#
		</isNotNull>
	</select>
	
	<select id="selectGameCount" resultClass="int" parameterClass="map">
		SELECT COUNT(*)
		FROM tbl_game g, tbl_league l
		WHERE g.LEAGUE_ID = l.ID
		<isNotNull prepend="AND" property="homeTeam">
			g.HOME_TEAM LIKE #homeTeam#
		</isNotNull>
		<isNotNull prepend="AND" property="awayTeam">
			g.AWAY_TEAM LIKE #awayTeam#
		</isNotNull>
		<isNotNull prepend="AND" property="type">
			g.TYPE = #type#
		</isNotNull>
		<isNotNull prepend="AND" property="leagueId">
			l.ID = #leagueId#
		</isNotNull>
		<isNotNull prepend="AND" property="status">
			g.STATUS = #status#
		</isNotNull>
		<isNotNull prepend="AND" property="statusList">
			<iterate prepend="" property="statusList" open="(" close=")" conjunction="OR">
				g.STATUS = #statusList[]#
			</iterate>
		</isNotNull>
		<isNotNull prepend="AND" property="betStatus">
			g.BET_STATUS = #betStatus#
		</isNotNull>
		<isNotNull prepend="AND" property="fromDate">
			g.GAME_DATE > #fromDate#
		</isNotNull>
		<isNotNull prepend="AND" property="fromDate">
			<![CDATA[
			g.GAME_DATE >= #fromDate#
			]]>
		</isNotNull>
		<isNotNull prepend="AND" property="toDate">
			<![CDATA[
			g.GAME_DATE <= #toDate#
			]]>
		</isNotNull>
	</select>
	
	<insert id="insertGame" parameterClass="Game">
		INSERT INTO tbl_game(HOME_TEAM, AWAY_TEAM, WIN_RATE, DRAW_RATE, LOSE_RATE, STATUS, GAME_DATE, LEAGUE_ID, TYPE, BET_STATUS)
			VALUES(#homeTeam#, #awayTeam#, #winRate#, #drawRate#, #loseRate#, #status#, #gameDate#, #leagueId#, #type#, #betStatus#)
	</insert>
	
	<update id="updateGame" parameterClass="Game">
		UPDATE tbl_game
		<dynamic prepend="SET">
			<isNotNull prepend="," property="homeTeam">
			HOME_TEAM = #homeTeam#
			</isNotNull>
			<isNotNull prepend="," property="awayTeam">
			AWAY_TEAM = #awayTeam#
			</isNotNull>
			<isNotNull prepend="," property="winRate">
			WIN_RATE = #winRate#
			</isNotNull>
			<isNotNull prepend="," property="drawRate">
			DRAW_RATE = #drawRate#
			</isNotNull>
			<isNotNull prepend="," property="loseRate">
			LOSE_RATE = #loseRate#
			</isNotNull>
			<isNotNull prepend="," property="gameDate">
			GAME_DATE = #gameDate#
			</isNotNull>
			<isNotNull prepend="," property="leagueId">
			LEAGUE_ID = #leagueId#
			</isNotNull>
			<isNotNull prepend="," property="type">
			TYPE = #type#
			</isNotNull>
			<isNotNull prepend="," property="status">
			STATUS = #status#
			</isNotNull>
			<isNotNull prepend="," property="betStatus">
			BET_STATUS = #betStatus#
			</isNotNull>
			<isNotNull prepend="," property="homeScore">
			HOME_SCORE = #homeScore# 
			</isNotNull>
			<isNotNull prepend="," property="awayScore">
			AWAY_SCORE = #awayScore# 
			</isNotNull>
			<isNotNull prepend="," property="result">
			RESULT = #result# 
			</isNotNull>
		</dynamic>
		WHERE ID = #id#
	</update>
	
	<update id="updateGameScoreNull" parameterClass="map">
		UPDATE tbl_game SET
			HOME_SCORE = #homeScore#,
			AWAY_SCORE = #awayScore#,
			RESULT = #result#,
			STATUS = #statue#
		WHERE ID = #id#		
	</update>
	
	<update id="updateGameRateNull" parameterClass="game">
		UPDATE tbl_game SET
			WIN_RATE = #winRate#
			DRAW_RATE = #drawRate#
			Lose_Rate = #loseRate# 
		WHERE ID = #id#		
	</update>
	
	<update id="batchGameBetStatus" parameterClass="date">
		<![CDATA[
		UPDATE tbl_game
		SET
			BET_STATUS = 'BTS02'
		WHERE
			GAME_DATE <= #date# AND
			BET_STATUS = 'BTS01'
		]]>			
	</update>
	
	<update id="batchGameStatus" parameterClass="date">
		<![CDATA[
		UPDATE tbl_game
		SET
			STATUS = 'GS003'
		WHERE
			GAME_DATE <= #date# AND
			STATUS = 'GS002'
		]]>			
	</update>
	
	<select id="selectGameWithBetMoney" resultMap="GameResultMap">
		SELECT
			ID, LEAGUE_ID, HOMETEAM, AWAYTEAM, GAME_DATE,
			(SELECT COUNT(*)
	</select>
</sqlMap>